import ToolTemplate from "@/components/ToolTemplate";

const AdCopyWriter = () => {
  return (
    <ToolTemplate
      title="Ad Copy Writer"
      description="Create compelling ad copy that converts"
      placeholder="Enter your product/service details and target audience (e.g., 'Fitness app for busy professionals')"
      toolType="adcopy"
    />
  );
};

export default AdCopyWriter;
