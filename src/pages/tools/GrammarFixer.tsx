import ToolTemplate from "@/components/ToolTemplate";

const GrammarFixer = () => {
  return (
    <ToolTemplate
      title="Grammar Fixer"
      description="Fix grammar errors and improve your writing instantly"
      placeholder="Paste your text here to check for grammar and spelling errors..."
      toolType="grammar"
    />
  );
};

export default GrammarFixer;
